From ff1b5fae3cbc2e22b86213145ba3b14d3537d907 Mon Sep 17 00:00:00 2001
From: John Rinehart <johnrichardrinehart@gmail.com>
Date: Thu, 3 Sep 2026 20:11:58 -0400
Subject: [PATCH] ssh: install terminfo before remote login

---
 README.md      |   9 +++
 dist/monstar.1 |  13 +++-
 src/Ssh.zig    | 157 +++++++++++++++++++++++++++++++++++++++++++++++++
 src/main.zig   |  35 ++++++++++-
 4 files changed, 211 insertions(+), 3 deletions(-)
 create mode 100644 src/Ssh.zig

diff --git a/README.md b/README.md
index fe14fb0..826923a 100644
--- a/README.md
+++ b/README.md
@@ -59,6 +59,15 @@ Pass `--` to run a shell expression:
 monstar -- 'git log --oneline | less'
 ```
 
+Install Monstar's terminfo entry and open an interactive SSH session:
+
+```sh
+monstar ssh user@example.com
+```
+
+The command accepts OpenSSH connection options before the destination. It does
+not accept a remote command or forwarding-only session options.
+
 Set the working directory, initial size, title, app ID, font, and one-off
 configuration overrides with command-line options:
 
diff --git a/dist/monstar.1 b/dist/monstar.1
index c3770c0..767d7ae 100644
--- a/dist/monstar.1
+++ b/dist/monstar.1
@@ -15,6 +15,10 @@ monstar \- a small Wayland terminal emulator
 .RI [ options ]
 .B \-\-
 .I command...
+.br
+.B monstar ssh
+.RI [ ssh-options ]
+.I destination
 .SH DESCRIPTION
 .B monstar
 is a Wayland terminal emulator written in Zig and built on Ghostty's terminal
@@ -121,11 +125,16 @@ Monstar uses Ghostty's
 terminal core and installs its own
 .B monstar
 terminfo entry.
-When a remote host does not recognize it, install the local definition there:
+Use
+.B monstar ssh
+to install the bundled entry before an interactive OpenSSH session:
 .PP
 .EX
-infocmp -x monstar | ssh HOST tic -x -
+monstar ssh [ssh-options] destination
 .EE
+.PP
+The command accepts OpenSSH connection options before the destination.
+It does not accept remote commands or forwarding-only session options.
 .SH CONFIGURATION
 The default configuration file is
 .I $XDG_CONFIG_HOME/monstar/config
diff --git a/src/Ssh.zig b/src/Ssh.zig
new file mode 100644
index 0000000..82df8dc
--- /dev/null
+++ b/src/Ssh.zig
@@ -0,0 +1,157 @@
+//! OpenSSH wrapper that installs Monstar's terminfo entry before login.
+
+const std = @import("std");
+
+const terminfo = @embedFile("monstar-terminfo-source");
+const bootstrap_command =
+    \\if infocmp monstar >/dev/null 2>&1; then
+    \\  cat >/dev/null
+    \\elif command -v tic >/dev/null 2>&1; then
+    \\  mkdir -p "$HOME/.terminfo" && tic -x -o "$HOME/.terminfo" -
+    \\else
+    \\  cat >/dev/null
+    \\  echo 'monstar ssh: remote host does not provide tic' >&2
+    \\  exit 127
+    \\fi
+;
+
+pub fn run(init: std.process.Init, args: []const [:0]const u8) !void {
+    const arena = init.arena.allocator();
+    const parsed = parseArgs(args) catch return error.InvalidCli;
+
+    const bootstrap_argv = try buildBootstrapArgs(arena, args, parsed);
+
+    var bootstrap = try std.process.spawn(init.io, .{
+        .argv = bootstrap_argv,
+        .stdin = .pipe,
+    });
+
+    bootstrap.stdin.?.writeStreamingAll(init.io, terminfo) catch |err| {
+        bootstrap.kill(init.io);
+        return err;
+    };
+    bootstrap.stdin.?.close(init.io);
+    bootstrap.stdin = null;
+    const bootstrap_term = try bootstrap.wait(init.io);
+    if (exitCode(bootstrap_term) != 0) return error.TerminfoBootstrapFailed;
+
+    var ssh_argv: std.ArrayList([]const u8) = .empty;
+    try ssh_argv.append(arena, "ssh");
+    for (args) |arg| try ssh_argv.append(arena, arg);
+    var ssh = try std.process.spawn(init.io, .{ .argv = ssh_argv.items });
+    const ssh_term = try ssh.wait(init.io);
+    std.process.exit(exitCode(ssh_term));
+}
+
+fn buildBootstrapArgs(
+    arena: std.mem.Allocator,
+    args: []const [:0]const u8,
+    parsed: ParsedArgs,
+) ![]const []const u8 {
+    var argv: std.ArrayList([]const u8) = .empty;
+    try argv.append(arena, "ssh");
+    try argv.append(arena, "-oRemoteCommand=none");
+    try argv.append(arena, "-oRequestTTY=no");
+    try argv.append(arena, "-oSessionType=default");
+    try argv.append(arena, "-oStdinNull=no");
+    for (args[0..parsed.destination_index], 0..) |arg, index| {
+        if (!parsed.omit_from_bootstrap[index]) try argv.append(arena, arg);
+    }
+    try argv.append(arena, args[parsed.destination_index]);
+    try argv.append(arena, bootstrap_command);
+    return argv.toOwnedSlice(arena);
+}
+
+const ParsedArgs = struct {
+    destination_index: usize,
+    omit_from_bootstrap: [256]bool = @splat(false),
+};
+
+fn parseArgs(args: []const [:0]const u8) !ParsedArgs {
+    if (args.len == 0 or args.len > 256) return error.InvalidCli;
+
+    var parsed: ParsedArgs = .{ .destination_index = undefined };
+    var index: usize = 0;
+    while (index < args.len) : (index += 1) {
+        const arg = args[index];
+        if (std.mem.eql(u8, arg, "--")) {
+            index += 1;
+            if (index >= args.len) return error.InvalidCli;
+            parsed.destination_index = index;
+            break;
+        }
+        if (arg.len == 0 or arg[0] != '-' or std.mem.eql(u8, arg, "-")) {
+            parsed.destination_index = index;
+            break;
+        }
+        if (arg.len < 2 or arg[1] == '-') return error.InvalidCli;
+
+        var omit_from_bootstrap = false;
+        var takes_value = false;
+        var short_index: usize = 1;
+        while (short_index < arg.len) : (short_index += 1) {
+            const option = arg[short_index];
+            if (std.mem.indexOfScalar(u8, "fGNsVQ", option) != null) return error.InvalidCli;
+            if (std.mem.indexOfScalar(u8, "nTt", option) != null) omit_from_bootstrap = true;
+            if (std.mem.indexOfScalar(u8, "BbCcDEeFIiJLlmOoPpRSwW", option) != null) {
+                takes_value = true;
+                if (short_index + 1 == arg.len) {
+                    index += 1;
+                    if (index >= args.len) return error.InvalidCli;
+                }
+                break;
+            }
+        }
+        if (omit_from_bootstrap and takes_value) return error.InvalidCli;
+        parsed.omit_from_bootstrap[index] = omit_from_bootstrap;
+    }
+
+    if (index >= args.len or index + 1 != args.len) return error.InvalidCli;
+    return parsed;
+}
+
+fn exitCode(term: std.process.Child.Term) u8 {
+    return switch (term) {
+        .exited => |code| code,
+        .signal => |signal| 128 +% @as(u8, @truncate(@intFromEnum(signal))),
+        .stopped, .unknown => 1,
+    };
+}
+
+test "SSH arguments find destination after options" {
+    const args = [_][:0]const u8{ "-p", "2222", "-vv", "user@example.com" };
+    const parsed = try parseArgs(&args);
+    try std.testing.expectEqual(3, parsed.destination_index);
+}
+
+test "SSH bootstrap omits terminal and stdin options" {
+    const args = [_][:0]const u8{ "-tn", "example.com" };
+    const parsed = try parseArgs(&args);
+    try std.testing.expect(parsed.omit_from_bootstrap[0]);
+}
+
+test "SSH bootstrap preserves connection options" {
+    var arena_state: std.heap.ArenaAllocator = .init(std.testing.allocator);
+    defer arena_state.deinit();
+
+    const args = [_][:0]const u8{ "-t", "-p", "2222", "user@example.com" };
+    const parsed = try parseArgs(&args);
+    const argv = try buildBootstrapArgs(arena_state.allocator(), &args, parsed);
+
+    try std.testing.expectEqualStrings("ssh", argv[0]);
+    try std.testing.expectEqualStrings("-oRemoteCommand=none", argv[1]);
+    try std.testing.expectEqualStrings("-p", argv[5]);
+    try std.testing.expectEqualStrings("2222", argv[6]);
+    try std.testing.expectEqualStrings("user@example.com", argv[7]);
+    try std.testing.expectEqualStrings(bootstrap_command, argv[8]);
+}
+
+test "SSH arguments reject remote commands" {
+    const args = [_][:0]const u8{ "example.com", "uptime" };
+    try std.testing.expectError(error.InvalidCli, parseArgs(&args));
+}
+
+test "SSH arguments reject forwarding-only sessions" {
+    const args = [_][:0]const u8{ "-N", "example.com" };
+    try std.testing.expectError(error.InvalidCli, parseArgs(&args));
+}
diff --git a/src/main.zig b/src/main.zig
index 4cee148..5f739b9 100644
--- a/src/main.zig
+++ b/src/main.zig
@@ -12,6 +12,7 @@ const Link = @import("Link.zig");
 const Pty = @import("Pty.zig");
 const Renderer = @import("Renderer.zig");
 const TerminalLayout = @import("TerminalLayout.zig");
+const Ssh = @import("Ssh.zig");
 const Window = @import("Window.zig");
 
 const log = std.log.scoped(.main);
@@ -28,6 +29,11 @@ pub fn main(init: std.process.Init) !void {
         .run => {},
         .bench => return @import("bench.zig").run(init),
         .help => return printUsage(init),
+        .ssh => return Ssh.run(init, cli.command) catch |err| switch (err) {
+            error.InvalidCli => invalidCli(init),
+            error.TerminfoBootstrapFailed => sshBootstrapFailed(init),
+            else => return err,
+        },
         .version => return printVersion(init),
     }
 
@@ -37,7 +43,7 @@ pub fn main(init: std.process.Init) !void {
     };
 }
 
-const CliAction = enum { run, bench, help, version };
+const CliAction = enum { run, bench, help, ssh, version };
 
 const CommandMode = enum { shell, exec };
 
@@ -79,6 +85,11 @@ const CliParser = struct {
             } else if (std.mem.eql(u8, arg, "--version")) {
                 self.cli.action = .version;
                 return self.cli;
+            } else if (self.index == 0 and std.mem.eql(u8, arg, "ssh")) {
+                if (self.index + 1 >= self.args.len) return error.InvalidCli;
+                self.cli.action = .ssh;
+                self.setCommand(.exec, self.index + 1);
+                break;
             } else if (std.mem.eql(u8, arg, "--")) {
                 if (self.index + 1 >= self.args.len) return error.InvalidCli;
                 self.setCommand(.shell, self.index + 1);
@@ -212,6 +223,7 @@ fn printUsage(init: std.process.Init) !void {
     try writer.interface.writeAll(
         \\usage: monstar [options]
         \\       monstar [options] -e command [args...]
+        \\       monstar ssh [ssh-options] destination
         \\       monstar [options] -- command...
         \\
         \\Options:
@@ -228,6 +240,7 @@ fn printUsage(init: std.process.Init) !void {
         \\  -o key=value                       Override a config key
         \\  -e command [args...]               Execute command directly
         \\  -- command...                      Run command through /bin/sh -c
+        \\  ssh [ssh-options] destination       Install terminfo, then connect with OpenSSH
         \\
     );
 }
@@ -247,6 +260,14 @@ fn invalidCli(init: std.process.Init) noreturn {
     std.process.exit(2);
 }
 
+fn sshBootstrapFailed(init: std.process.Init) noreturn {
+    var buf: [256]u8 = undefined;
+    var writer = std.Io.File.stderr().writer(init.io, &buf);
+    writer.interface.writeAll("monstar ssh: failed to install remote terminfo\n") catch {};
+    writer.interface.flush() catch {};
+    std.process.exit(1);
+}
+
 /// GUI mode: run a live terminal session in a window.
 fn gui(init: std.process.Init, cli: CliOptions) !void {
     const arena = init.arena.allocator();
@@ -482,6 +503,18 @@ test "parse CLI reserves bare words for future subcommands" {
     try std.testing.expectError(error.InvalidCli, parseCli(arena_state.allocator(), &args));
 }
 
+test "parse CLI accepts SSH subcommand" {
+    var arena_state: std.heap.ArenaAllocator = .init(std.testing.allocator);
+    defer arena_state.deinit();
+
+    const args = [_][:0]const u8{ "ssh", "-p", "2222", "user@example.com" };
+    const cli = try parseCli(arena_state.allocator(), &args);
+
+    try std.testing.expectEqual(.ssh, cli.action);
+    try std.testing.expectEqualStrings("-p", cli.command[0]);
+    try std.testing.expectEqualStrings("user@example.com", cli.command[2]);
+}
+
 test "parse CLI double dash command uses shell mode" {
     var arena_state: std.heap.ArenaAllocator = .init(std.testing.allocator);
     defer arena_state.deinit();
-- 
2.54.0

